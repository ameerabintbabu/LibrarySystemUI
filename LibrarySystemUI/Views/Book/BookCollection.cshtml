@model IEnumerable<LibrarySystemUI.Models.Book>

@{
    ViewBag.Title = "BookCollection";
}

<h2>BookCollection</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
<select id="filterParentCategory" data-placeholder="" style="width:12%;">
    <option value='0' class='selected'>Select Category</option>
</select>
<select id="filterSubCategory" data-placeholder="" style="width:12%;">
    <option value='0' class='selected'>Select SubCategory</option>
</select>
<button type="button" class="btn btn-default btn-success" data-dismiss="modal" style="float:right;" onclick="objBook.RedirectToQuestion()">Get Last 50 questions from stack overflow</button>
@using (Html.BeginForm())
{
    <table class="table" id="tbl_BookData">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BookName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubCategory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Publication)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoOfCopies)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.BookName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Category)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SubCategory)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Publication)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NoOfCopies)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                @*<td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.BookId }) |
                        @Html.ActionLink("Details", "Details", new { id = item.BookId }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.BookId })
                    </td>*@
            </tr>
        }

    </table>
}
<script src="~/Scripts/Book/Book.js"></script>
<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<script src="~/Scripts/bootstrap.min.js"></script>
<script>
    $(document).ready(function () {


        objBook.init();

    });
</script>